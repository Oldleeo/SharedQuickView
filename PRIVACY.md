# 共享速览隐私说明

生效日期：2026-09-03

发布者：老李Oldlee

## 结论

共享速览在本机运行，不提供云端账号，不包含广告、统计 SDK 或遥测功能，也不会把目标 IP、共享路径或剪贴板内容上传到作者或第三方服务器。

## 本地处理的数据

软件可能在本机处理以下内容：

1. 用户填写的目标 IPv4 地址；
2. 用户主动粘贴或复制的 UNC 共享路径、网址或本地磁盘路径；
3. 开机启动、窗口置顶、悬浮窗和打开行为等偏好；
4. 窗口位置。

上述内容仅用于完成路径识别、IP 替换、界面恢复和用户选择的自动化行为。

## 剪贴板

“复制有效路径后自动识别并打开”默认关闭。开启后，软件会在本机监听新复制的文字，并判断是否包含可打开内容。网址和本地磁盘路径识别另有独立开关，且同样默认关闭。

- 不符合格式的文字不会触发打开；
- 剪贴板内容不会写入日志或上传；
- 退出软件或关闭该设置后停止自动识别。

## 设置文件

个人设置默认保存在：

```text
%AppData%\Oldlee\共享速览\settings.json
```

卸载时可以选择保留或删除该设置文件。

## 网络行为

软件自身没有遥测服务器。发生网络访问的场景仅包括：

1. Windows 根据用户提供的 UNC 路径连接相应 SMB 共享服务器；
2. 用户主动点击 `@老李Oldlee` 时，默认浏览器访问 `https://x.com/oldleeoo`；
3. 用户开启网址识别后，Windows 会使用默认浏览器访问用户复制或粘贴的 `http/https` 网址。

共享服务器的身份验证、权限和日志由用户所在组织及 Windows 管理，不由本项目控制。

## 公开反馈时的隐私保护

提交 GitHub Issue 前，请隐藏或替换：

- 真实内网或公网 IP；
- 服务器名和共享名；
- 用户名、公司名和客户名；
- 内部目录结构；
- Windows 凭据和访问令牌；
- 含敏感信息的截图。

推荐使用 `192.168.1.100`、`共享资料`、`设计部` 等虚构内容复现问题。

## 联系方式

项目主页：<https://github.com/Oldleeo/SharedQuickView>

作者主页：<https://x.com/oldleeoo>
